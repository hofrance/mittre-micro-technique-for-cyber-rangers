[package]
name = "t1595_026z-active_scanning-ftp_protocol_scan-linux"
version = "1.0.0"
description = "Micro-technique for FTP protocol reconnaissance and enumeration"
authors = ["BANKOUEZI  Richard   <contact@richard-hofrance.com> "]
license = "MIT"
categories = ["MITRE ATT&CK", "linux", "reconnaissance", "t1595.026z", "ftp-scanning", "user"]
readme = "README.md"

assets = [
    ["src/main.sh", "/tmp/mitre_results", "755"],
]
[content]
type = "inject"
preview = [
    {type = "code", value = ["src/main.sh"]}
]

[inject]
action = "/tmp/mitre_results/${PACKAGE_NAME}.sh"
restarts = false
