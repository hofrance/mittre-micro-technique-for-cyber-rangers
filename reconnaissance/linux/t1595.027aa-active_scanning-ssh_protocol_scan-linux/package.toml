[package]
name = "t1595_027aa-active_scanning-ssh_protocol_scan-linux"
version = "1.0.0"
description = "Micro-technique for SSH protocol reconnaissance and enumeration"
authors = ["BANKOUEZI  Richard   <contact@richard-hofrance.com> "]
license = "MIT"
categories = ["MITRE ATT&CK", "linux", "reconnaissance", "t1595.027aa", "ssh-scanning", "user"]
readme = "README.md"

assets = [
    ["src/main.sh", "/tmp/mitre_results", "755"],
]
[content]
type = "inject"
preview = [
    {type = "code", value = ["src/main.sh"]}
]

[inject]
action = "/tmp/mitre_results/${PACKAGE_NAME}.sh"
restarts = false
